// This file is automatically generated. Don't edit. Created 2022-10-29 19:45:27.
import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { subsToUrl } from './subs-to-url.func';
import { TablesDTO, None } from './dto.models';

@Injectable({
  providedIn: 'root',
})
export class MasterApiService {
  constructor(private http: HttpClient) {}

  helloWorld(): Observable<None> {
    const url = subsToUrl('/api/', {});
    return this.http.get<None>(url);
  }

  getTables(): Observable<TablesDTO> {
    const url = subsToUrl('/api/table/list', {});
    return this.http.get<TablesDTO>(url);
  }
}
